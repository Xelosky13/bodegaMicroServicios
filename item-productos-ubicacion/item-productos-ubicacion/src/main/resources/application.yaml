spring:
    application:
        name: item-productos-ubicacion

    datasource:
        url: jdbc:mysql://localhost:3306/db-bodega
        username: root
        password: ""

    jpa:
        hibernate:
            naming:
                physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

server:
    port: 8081

springdoc:
  swagger-ui:
    path: /doc/swagger-ui/index.html
  api-docs:
    path: /api-docs


